<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jeas.netapp">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/game_logo1"
        android:label="Guesser"
        android:supportsRtl="true"
        android:theme="@style/SplashActivityTheme">
        <activity
            android:name=".media.Start_Page"
            android:theme="@style/SplashActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/SplashActivityTheme" />
        <activity android:name=".into_game_activity" />
        <activity android:name=".into_invite_friends" />
        <activity android:name=".setting" />
        <activity android:name=".help" />
        <activity android:name=".AboutUs" />
        <activity android:name=".media.music_adjust" />

        <!-- <service -->
        <!-- android:name=".InternetService" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true" /> -->

        <activity android:name=".media.Draw" />
        <activity android:name=".Chat"></activity>
    </application>

</manifest>